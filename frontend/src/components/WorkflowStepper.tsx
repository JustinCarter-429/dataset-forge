import { Check } from 'lucide-react'
export function WorkflowStepper({ step }: { step: number }) { return <div className="workflow-stepper" aria-label="Dataset workflow">{['Upload', 'Describe', 'Generate'].map((label, index) => <div className="step-wrap" key={label}><div className={`step-node ${index < step ? 'complete' : index === step ? 'current' : ''}`} aria-current={index === step ? 'step' : undefined}>{index < step ? <Check size={15} /> : index + 1}</div><span className={index <= step ? 'step-label-active' : ''}>{label}</span>{index < 2 && <div className={`step-connector ${index < step ? 'connector-active' : ''}`} />}</div>)}</div> }
