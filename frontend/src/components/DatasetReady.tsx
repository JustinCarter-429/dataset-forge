import { ArrowDownToLine, CheckCircle2, FileArchive, RotateCcw } from 'lucide-react'
import type { GenerationResult } from '../api/types'

export function DatasetReady({ result, onReset, onDownload }: { result: GenerationResult, onReset: () => void, onDownload: () => void }) {
  return <section className="ready-card"><div className="ready-heading"><div className="ready-icon"><CheckCircle2 size={26} /></div><div><p className="eyebrow">Generation complete</p><h2>Dataset Ready</h2></div></div><p className="ready-message">Your Carter 1.0 dataset has been validated and packaged for download.</p><div className="result-grid"><div><span>Records</span><strong>{result.record_count} records</strong></div><div><span>Format</span><strong>{result.output_format.toUpperCase()}</strong></div><div><span>Source</span><strong>{result.source_filename}</strong></div><div><span>Archive</span><strong><FileArchive size={15} /> {result.archive_filename}</strong></div></div><div className="ready-actions"><button className="primary-button" onClick={onDownload}><ArrowDownToLine size={18} /> Download ZIP</button><button className="secondary-button" onClick={onReset}><RotateCcw size={17} /> Create Another Dataset</button></div></section>
}
