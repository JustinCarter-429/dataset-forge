import { Check, Braces, Table2, Text } from 'lucide-react'
import type { OutputFormat } from '../api/types'
export function FormatSelector({ value, onChange }: { value: OutputFormat, onChange: (value: OutputFormat) => void }) { return <div className="format-grid">{([['jsonl', 'JSONL', 'Fine-tuning: one example per line', Text], ['json', 'JSON', 'Human-readable structured dataset', Braces], ['csv', 'CSV', 'Tabular format', Table2] ] as const).map(([id, label, copy, Icon]) => <button key={id} className={`format-option ${value === id ? 'format-option-active' : ''}`} onClick={() => onChange(id)}><Icon size={21} /><span><strong>{label}</strong><small>{copy}</small></span>{value === id && <Check className="format-check" size={18} />}</button>)}</div> }
