# Native rule v2

`native-rule-v2` uses only authored fictional source families. It groups every renderer and mutation from a source family into one deterministic split, retains original canonical values, and validates every proof-carrying record. The held-out test has complete source-family separation. Inputs contain no internal decision, mutation, proof, or issue-code vocabulary. It is rule-derived supervision only; teacher labeling remains unavailable.
