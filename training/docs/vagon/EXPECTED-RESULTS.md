# Expected results

Successful GPU certification reports `GPU_SMOKE_CERTIFIED` and `WP5B_READY`. A blocked certification reports `GPU_CERTIFICATION_BLOCKED` and `WP5B_NOT_READY`. A 10-step smoke validates plumbing; it is not a final trained critic.
