# Example-only future training configuration. Values are intentionally not final.
config_version: "1.0.0"
seed: 42
splits:
  train: train
  validation: validation
  test: test
max_sequence_length: 2048 # TODO: validate against model and dataset requirements.
batch_size: 1 # TODO: select for available hardware.
gradient_accumulation_steps: 1 # TODO: select for available hardware.
learning_rate: 0.0001 # TODO: tune in a later work package.
epochs: 1 # TODO: establish after dataset inspection.
lora:
  enabled: true # Placeholder only; WP1 does not install or configure PEFT.
  rank: 8
  alpha: 16
checkpoint_directory: checkpoints/
output_directory: outputs/
